; Igual que el output_buffering por defecto de XAMPP: evita que espacios en
; blanco antes de header() rompan los redirects (p.ej. tras el login).
output_buffering = 4096
display_errors = Off
log_errors = On
